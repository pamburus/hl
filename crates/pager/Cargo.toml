[package]
name = "pager"
version = "0.1.0"
edition.workspace = true
description = "Pager process management for terminal output"
repository.workspace = true
license.workspace = true

[dependencies]
log = "0.4"
shellwords = "1"
