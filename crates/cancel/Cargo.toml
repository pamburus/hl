[package]
name = "cancel"
version = "0.1.0"
edition.workspace = true
description = "Cooperative cancellation primitives"
repository.workspace = true
license.workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"
