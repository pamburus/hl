# Test file for FR-030b: Theme stem deduplication
# This TOML version should be deduplicated with dedup-test.yaml
# When listing themes, "dedup-test" should appear only once

version = "0.0"

[elements.key]
foreground = "magenta"

[elements.message]
foreground = "blue"
