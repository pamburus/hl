# V0 theme without input element defined
# This should fall back to @base.toml's input element

elements:
  message:
    foreground: "bright-white"
    modes: ["bold"]

  level:
    foreground: "green"

  time:
    foreground: "bright-black"

  caller:
    foreground: "bright-black"
    modes: ["italic"]
