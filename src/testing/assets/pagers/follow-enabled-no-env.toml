# Test config: profile with follow mode explicitly enabled
# No env candidates - for testing pure profile selection
[pager]
candidates = [{ profile = "fzf" }]

[pager.profiles.fzf]
command = ["fzf", "--ansi"]
view.args = ["--layout=reverse-list"]
follow.enabled = true
follow.args = ["--tac", "--track"]
