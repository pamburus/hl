[package]
name = "heapopt"
version = "0.1.1"
edition.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
derive-where = "1"
heapless = "0"

[dev-dependencies]
more-asserts = "0"
